name: PR checks (develop)

on:
    pull_request:
        branches:
            - develop
        paths-ignore:
            - '.vscode/**'
            - '.claude/**'
            - '.gitignore'
            - '**/*.md'
            - '.github/workflows/!(pr-checks).yml'
            - 'scripts/**'
            - 'docs/**'
    workflow_dispatch: # テスト用の手動実行

jobs:
    # 変更検出ジョブ - 各サービスディレクトリの変更を検出
    detect-changes:
        runs-on: ubuntu-latest
        timeout-minutes: 5
        outputs:
            django-api: ${{ steps.filter.outputs.django-api }}
            sbv2-api: ${{ steps.filter.outputs.sbv2-api }}
            docker-compose: ${{ steps.filter.outputs.docker-compose }}
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Detect changes
              uses: dorny/paths-filter@v3
              id: filter
              with:
                  filters: |
                      django-api:
                        - 'django_api/**'
                      sbv2-api:
                        - 'sbv2_api/**'
                      docker-compose:
                        - 'docker-compose.yml'
                        - '.env.sample'

    # Dockerfile静的解析ジョブ（セキュリティスキャン）
    dockerfile-security-scan:
        needs: detect-changes
        if: |
            needs.detect-changes.outputs.django-api == 'true' ||
            needs.detect-changes.outputs.sbv2-api == 'true' ||
            github.event_name == 'workflow_dispatch'
        runs-on: ubuntu-latest
        timeout-minutes: 5
        strategy:
            fail-fast: false
            matrix:
                include:
                    - name: django-api
                      path: ./django_api
                      changed: django-api
                    - name: sbv2-api
                      path: ./sbv2_api
                      changed: sbv2-api
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Run Trivy config scan (${{ matrix.name }})
              if: needs.detect-changes.outputs[matrix.changed] == 'true' || github.event_name == 'workflow_dispatch'
              uses: aquasecurity/trivy-action@0.35.0
              with:
                  scan-type: 'config'
                  scan-ref: ${{ matrix.path }}
                  exit-code: '1'
                  severity: 'CRITICAL,HIGH'
                  format: 'table'

    # Django APIテストジョブ（django_apiに変更がある場合のみ実行）
    django-api-test:
        needs: detect-changes
        if: needs.detect-changes.outputs.django-api == 'true' || github.event_name == 'workflow_dispatch'
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: '3.13'

            - name: Install uv
              run: |
                  pip install --upgrade pip
                  pip install uv

            - name: Install dependencies
              working-directory: ./django_api
              run: |
                  uv pip install --system .
                  uv pip install --system pytest pytest-django pytest-cov pytest-mock factory-boy faker ruff

            - name: Run ruff linter
              working-directory: ./django_api
              run: |
                  echo "🔍 Ruffリンターチェック実行中..."
                  uv run ruff check .
                  echo "✅ リンターチェック完了"

            - name: Run ruff formatter check
              working-directory: ./django_api
              run: |
                  echo "🎨 Ruffフォーマッターチェック実行中..."
                  uv run ruff format --check .
                  echo "✅ フォーマッターチェック完了"

            - name: Create test environment
              working-directory: ./django_api
              run: |
                  # テスト用.envファイル作成（.env.sampleに準拠）
                  echo "SECRET_KEY=test-secret-key-for-ci-testing" > .env
                  echo "DEBUG=False" >> .env
                  echo "ALLOWED_HOSTS=localhost,testserver" >> .env
                  echo "ENCRYPTION_KEY=test-encryption-key-for-ci-testing" >> .env

            - name: Run tests with coverage
              working-directory: ./django_api
              env:
                  DJANGO_SETTINGS_MODULE: django_api.settings
              run: |
                  echo "🧪 テスト実行中..."
                  python -m pytest tests/ -v --tb=short \
                    --cov=user --cov=onedrive --cov=outlook --cov=core \
                    --cov-report=term-missing \
                    --cov-report=html \
                    --cov-fail-under=80 \
                    -m "not e2e"
                  echo "✅ テスト完了"

            - name: Upload coverage report
              if: always()
              uses: actions/upload-artifact@v4
              with:
                  name: django-coverage-report
                  path: django_api/htmlcov/

    # PRチェック用のメインジョブ（いずれかのサービスに変更がある場合のみ実行）
    container-integration-tests:
        needs: detect-changes
        if: |
            needs.detect-changes.outputs.django-api == 'true' ||
            needs.detect-changes.outputs.docker-compose == 'true' ||
            github.event_name == 'workflow_dispatch'
        runs-on: ubuntu-latest
        timeout-minutes: 15

        steps:
            # ソースコードの取得
            - name: Checkout
              uses: actions/checkout@v4

            # CI環境用のDjango API環境変数ファイル作成
            - name: Create Django API .env file for CI
              run: |
                  cp django_api/.env.sample django_api/.env
                  echo "SECRET_KEY=test-secret-key-for-ci-environment" >> django_api/.env
                  echo "DEBUG=False" >> django_api/.env
                  echo "ALLOWED_HOSTS=localhost" >> django_api/.env
                  echo "ENCRYPTION_KEY=test-encryption-key-for-ci-environment" >> django_api/.env

            # 永続化ディレクトリの準備（コンテナマウント用）
            - name: Prepare volume directories
              run: |
                  sudo mkdir -p /opt/app/django-api/staticfiles
                  sudo touch /opt/app/django-api/db.sqlite3
                  sudo chmod -R 777 /opt/app/

            # Dockerイメージのビルド
            - name: Build Docker images
              run: docker compose build --pull django-api

            # Django API: マイグレーション確認
            - name: Test Django migrations
              run: |
                  echo "🔧 Django マイグレーション確認..."
                  # CI環境用：rootユーザーで実行して権限問題を回避
                  docker compose run --rm --no-deps -u root -w /django_api django-api sh -c "
                    python manage.py migrate --run-syncdb && \
                    python manage.py migrate --check
                  "
                  echo "✅ マイグレーション: OK"

            # Django API: 静的ファイル収集確認
            - name: Test Django collectstatic
              run: |
                  echo "📦 Django 静的ファイル収集確認..."
                  # システムPythonにインストールされたパッケージを使用（UV_SYSTEM_PYTHON=1が設定済み）
                  docker compose run --rm --no-deps -w /django_api django-api sh -c "python manage.py collectstatic --noinput --dry-run"
                  echo "✅ 静的ファイル: OK"

            # コンテナの起動
            - name: Start containers
              run: |
                  echo "🚀 コンテナ起動中..."
                  docker compose up -d

                  echo "⏳ コンテナ準備完了待機中..."
                  sleep 10  # 初期化待ち

                  # コンテナ状態確認
                  echo "📋 コンテナ状態:"
                  docker compose ps

            # 準備完了待機（django-api）
            - name: Wait for readiness :django-api
              run: |
                  # Django APIのヘルスチェック
                  echo "🔍 Django API確認中..."
                  api_ready=false
                  for i in {1..30}; do
                    # コンテナが起動中かチェック
                    if docker ps --filter "name=django-api" --filter "status=running" | grep -q django-api; then
                      # curlで確認
                      if docker exec django-api sh -c "command -v curl >/dev/null 2>&1 && curl -s http://localhost:8000/schema/ 2>/dev/null | head -n1 | grep -q openapi" 2>/dev/null; then
                        echo "✅ Django API準備完了 (curl使用)"
                        api_ready=true
                        break
                      # wgetで確認
                      elif docker exec django-api sh -c "command -v wget >/dev/null 2>&1 && wget -q -O - http://localhost:8000/schema/ 2>/dev/null | head -n1 | grep -q openapi" 2>/dev/null; then
                        echo "✅ Django API準備完了 (wget使用)"
                        api_ready=true
                        break
                      # pythonで確認
                      elif docker exec django-api sh -c "python -c 'import urllib.request; print(urllib.request.urlopen(\"http://localhost:8000/schema/\").read().decode()[:100])' 2>/dev/null | grep -q openapi" 2>/dev/null; then
                        echo "✅ Django API準備完了 (Python urllib使用)"
                        api_ready=true
                        break
                      fi
                    fi
                    echo "  [$i/30] 待機中..."
                    sleep 3
                  done
                  # タイムアウトチェック
                  if [ "$api_ready" = false ]; then
                    echo "⚠️ Django API準備確認タイムアウト（後続テストで動作確認）"
                  fi

            # Django API機能テスト
            - name: Django API functionality tests
              run: |
                  echo "🧪 Django API機能テスト実行中..."

                  # OpenAPIスキーマ確認
                  echo "  📄 OpenAPIスキーマ... (localhost:8000/schema/)"
                  content=$(curl -s http://localhost:8000/schema/ | head -n1)
                  if echo "$content" | grep -q "openapi"; then
                    echo "  ✅ スキーマ: OK"
                  else
                    echo "  ❌ スキーマ: NG"
                    exit 1
                  fi

                  # Swagger UI確認
                  echo "  🎨 Swagger UI... (localhost:8000/swagger/)"
                  status=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/swagger/)
                  if [ "$status" = "200" ]; then
                    echo "  ✅ Swagger UI: OK"
                  else
                    echo "  ❌ Swagger UI: NG (HTTP $status)"
                    exit 1
                  fi

            # エラー時のデバッグ情報
            - name: Debug info on failure
              if: failure()
              run: |
                  echo "🚨 === デバッグ情報 ==="
                  echo "📋 コンテナ状態:"
                  docker compose ps
                  echo ""
                  echo "📋 django-api ログ:"
                  docker compose logs django-api | tail -n 50

            # 環境のクリーンアップ（常に実行）
            - name: Cleanup
              if: always()
              run: |
                  docker compose down -v
                  echo "🧹 クリーンアップ完了"
